# Hand-routing worklist

1 connections left, generated by `tools/gen_worklist.py` from the DRC report on `NAVCORE-SoOP.kicad_pcb`.

Ordered the way `docs/LAYOUT.md` says to route them: the nets where
geometry affects function come first, bulk power last. **In1.Cu and In2.Cu are planes - do not route on them.** Use F.Cu and B.Cu, and drop
a via to the plane rather than running a rail across a signal layer.


## buck switch (1)

| net | from | to | near |
|---|---|---|---|
| `BUCK9_PH` | U18.2 | track | (130.5, 122.4) |
